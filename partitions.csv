# =============================================================================
#  LidarAGL — partition table
# -----------------------------------------------------------------------------
#  Single-app layout for the 4 MB flash on the fitted ESP32-S3. The voice clips
#  are EMBEDDED in the app image (EMBED_FILES), NOT stored in a data partition:
#    - nvs       : boot-buffer ground reference + saved config (wear-leveled).
#    - phy_init  : RF calibration data (kept for completeness; radios are off).
#    - factory   : the application image (~1.04 MB today; 1.75 MB leaves ~700 KB
#                  of headroom for growth).
#    - flightlog : the black-box recorder ring (flightlog.c). Everything from
#                  the end of the app slot to the end of flash, ~2.2 MB. Read it
#                  out with tools/flightlog/flightlog.py.
#
#  nvs and phy_init keep their ORIGINAL offsets, so re-flashing this table does
#  not disturb a unit's saved configuration or learned ground reference.
#  'undefined' is ESP-IDF's application-defined data subtype; the firmware finds
#  the recorder by its LABEL, which must match FLOG_PARTITION_LABEL (config.h).
# =============================================================================
# Name,     Type, SubType,   Offset,   Size
nvs,        data, nvs,       0x9000,   0x6000
phy_init,   data, phy,       0xf000,   0x1000
factory,    app,  factory,   0x10000,  0x1C0000
flightlog,  data, undefined, 0x1D0000, 0x230000
